{% block subject %}{{ 'mail.resetting_password.subject'|trans({}, 'text') }}{% endblock %}

{% block body_text %}
  {% autoescape false %}
    {{ 'mail.resetting_password.content'|trans({
      '%url%'      : confirmationUrl,
      '%username%' : user.username
    }, 'text') }}
    {{ 'mail.general.footer'|trans({}, 'text') }}
  {% endautoescape %}
{% endblock %}